{
  "name": "connect-and-conquer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Connect & Chain — a juicy, cartoony mobile chain-and-prune prototype (vanilla canvas + Vite, Capacitor-ready).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cap:sync": "cap sync",
    "cap:add:ios": "cap add ios",
    "cap:add:android": "cap add android"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2",
    "vite": "^5.4.10"
  }
}
